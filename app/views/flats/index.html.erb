<div class="container mt-5">
  <h1>All our Flats</h1>
  <p>All <%= Flat.count %> flats</p>
  <div class="row text-center mt-3">
    <% @flats.each do |flat| %>
      <div class="col-md-4">
        <%= render flat %>
      </div>
    <% end %>
  </div>
</div>
